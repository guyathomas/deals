# Setup TODO

- [ ] Install Playwright browsers: `npx playwright install chromium`
- [ ] Test locally: `node src/index.js scrape -s jcrew`
- [ ] Add `ADMIN_TOKEN` as a GitHub Actions secret for the daily cron
